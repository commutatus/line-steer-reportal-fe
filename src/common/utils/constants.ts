export enum RoutePaths {
  HOME = "/",
  PLANNING = "/consumer",
  REQUESTS = "/generator",
  REPORTS = "/reports",
  DAY_WISE_PLAN = "/reports/day-wise-plan",
  OVERALL_PLAN = "/reports/overall-plan",
  LOAD_GRAPH = "/reports/load-graph",
  BILLING = "/billing",
  RECS = "/recs",
}
